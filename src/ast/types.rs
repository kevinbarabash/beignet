// TODO: add variants to model various types

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum TypeAnn {
    // copied from src/types.rs
    // Var(VarType),
    // Lam(LamType),
    // Prim(PrimType),
    // Lit(LitType),
    // Union(UnionType),
    // Object(ObjectType),
    // Alias(AliasType),
}
